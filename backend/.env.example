# Optional: set to openai to enable OpenAI-assisted policy interpretation.
# Keep rule_based (or omit it) to use the built-in local interpreter only.
POLICYFORGE_AI_MODE=rule_based

# Never expose this key in the frontend or commit a real value.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
